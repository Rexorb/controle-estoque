package com.estoque.controle_estoque.enums;

public enum TipoMovimentacao {
    ENTRADA, // Representa o aumento do estoque (ex: compra, devolução)
    SAIDA    // Representa a diminuição do estoque (ex: venda, perda)
}